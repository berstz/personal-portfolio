# Production Configuration

NODE_ENV=production

# Database (SQLite for simplicity, consider PostgreSQL/MySQL for production)
DATABASE_URL=file:./prisma/dev.db

# Session (REQUIRED - must be at least 32 characters)
# Generate with: openssl rand -base64 32
SESSION_PASSWORD=change-this-to-a-secure-session-password-at-least-32-characters

# Admin credentials (used by npm run db:seed)
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=ChangeThisToASecurePassword2024!

# Port (optional, defaults to 3000)
PORT=3000

